name: feed_app
description: feed_app

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  feed_lib:
    path: ../feed_lib

  go_router: ^3.1.1
  validators: ^3.0.0
  http: ^0.13.4
  provider: ^6.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  feed_test_lib:
      path: ../feed_test_lib

  flutter_lints: ^2.0.1
  build_runner: ^2.1.11
  json_serializable: ^6.2.0
  mocktail: ^0.3.0
  network_image_mock: ^2.1.0

flutter:
  uses-material-design: true
